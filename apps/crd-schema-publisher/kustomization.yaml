apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: crd-schema
  namespace: crd-schema
resources:
  - resources/httpproxy.yaml

helmCharts:
  - name: crd-schema
    # version: "v2026.609.12656"
    repo: "oci://ghcr.io/sholdee/charts/crd-schema-publisher"
    releaseName: "crd-schema"
    namespace: crd-schema
    valuesFile: "helm/values.yaml"
