apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: outline-cedille2
spec:
  virtualhost:
    fqdn: cedille2.etsmtl.club
    tls:
      secretName: outline-cedille2-tls
      minimumProtocolVersion: "1.3"
  routes:
    - conditions:
        - prefix: /
      enableWebsockets: true
      services:
        - name: outline-cedille2
          port: 3000
      timeoutPolicy:
        idle: 600s
        response: 600s
