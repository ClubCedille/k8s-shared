apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dnf-cache
spec:
  podSelector:
    matchLabels:
      app: dnf-cache
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8080
          protocol: TCP
