apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: freebsd-pkg-cache
spec:
  podSelector:
    matchLabels:
      app: freebsd-pkg-cache
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8080
          protocol: TCP
