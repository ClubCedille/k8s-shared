apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: netbox-tls
spec:
  secretName: netbox-tls
  issuerRef:
    name: letsencrypt-etsmtl
    kind: ClusterIssuer
  commonName: netbox.etsmtl.club
  dnsNames:
    - netbox.etsmtl.club
