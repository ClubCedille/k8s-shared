apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crabe-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-http
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/tls-acme: "true"
spec:
  ingressClassName: contour
  tls:
    - secretName: crabe-tls
      hosts:
        - atelier.crabe.prodv2.cedille.club
    - secretName: crabe-etsmtl-tls
      hosts:
        - atelier.crabe.etsmtl.club
  rules:
    - host: atelier.crabe.prodv2.cedille.club
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: crabe-svc
                port:
                  number: 8000
    - host: atelier.crabe.etsmtl.club
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: crabe-svc
                port:
                  number: 8000
