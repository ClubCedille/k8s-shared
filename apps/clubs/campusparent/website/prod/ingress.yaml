apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: campusparent
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-http
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/tls-acme: "true"
spec:
  ingressClassName: contour
  tls:
    - secretName: campusparent-tls
      hosts:
        - campusparent.etsmtl.club
  rules:
    - host: campusparent.etsmtl.club
      http:
        paths:
          - pathType: Prefix
            path: /api
            backend:
              service:
                name: campusparent-backend-svc
                port:
                  number: 3000
          - pathType: Prefix
            path: /
            backend:
              service:
                name: campusparent-svc
                port:
                  number: 80
