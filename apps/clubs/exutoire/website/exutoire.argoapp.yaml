apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: exutoire
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: webapp=ghcr.io/clubcedille/exutoire:latest
    argocd-image-updater.argoproj.io/webapp.update-strategy: digest
    argocd-image-updater.argoproj.io/webapp.pull-secret: pullsecret:argocd/ghcr-clubcedille-secret
spec:
  project: k8s-shared
  destination:
    server: https://kubernetes.default.svc
    namespace: exutoire
  source:
    repoURL: https://github.com/ClubCedille/k8s-shared
    path: apps/clubs/exutoire/website/prod
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
