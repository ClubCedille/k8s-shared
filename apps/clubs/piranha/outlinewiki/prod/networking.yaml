apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: piranha-outlinewiki-tls
spec:
  secretName: piranha-outlinewiki-tls
  issuerRef:
    name: letsencrypt-etsmtl
    kind: ClusterIssuer
  commonName: wiki.piranha.etsmtl.club
  dnsNames:
    - wiki.piranha.etsmtl.club
---
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: piranha-outlinewiki-httpproxy
spec:
  virtualhost:
    fqdn: wiki.piranha.etsmtl.club
    tls:
      secretName: piranha-outlinewiki-tls
      minimumProtocolVersion: "1.3"
  routes:
    - conditions:
        - prefix: /
      enableWebsockets: true
      services:
        - name: piranha-outlinewiki-svc
          port: 3000
      timeoutPolicy:
        idle: 600s
        response: 600s
