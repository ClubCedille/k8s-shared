---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-shared-nextcloud
  namespace: argocd
spec:
  destination:
    namespace: nextcloud
    server: https://kubernetes.default.svc
  project: k8s-shared
  sources:
    - path: apps/nextcloud
      repoURL: https://github.com/ClubCedille/k8s-shared.git
      targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions: [CreateNamespace=true]
