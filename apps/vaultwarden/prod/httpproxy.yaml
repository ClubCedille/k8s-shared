apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: vaultwarden-httpproxy
spec:
  virtualhost:
    fqdn: vaultwarden.etsmtl.club
    tls:
      secretName: vaultwarden-tls
      minimumProtocolVersion: "1.3"
  routes:
    - conditions:
        - prefix: /
      services:
        - name: vaultwarden
          port: 80
      enableWebsockets: true
---
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: vaultwarden-httpproxy-pwd
spec:
  virtualhost:
    fqdn: pwd.etsmtl.club
    tls:
      secretName: vaultwarden-tls-pwd
      minimumProtocolVersion: "1.3"
  routes:
    - conditions:
        - prefix: /
      services:
        - name: vaultwarden
          port: 80
      enableWebsockets: true
---
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: vaultwarden-httpproxy-mdp
spec:
  virtualhost:
    fqdn: mdp.etsmtl.club
    tls:
      secretName: vaultwarden-tls-mdp
      minimumProtocolVersion: "1.3"
  routes:
    - conditions:
        - prefix: /
      services:
        - name: vaultwarden
          port: 80
      enableWebsockets: true
