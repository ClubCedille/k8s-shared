apiVersion: v1
kind: Service
metadata:
  name: littlelink-service
spec:
  ports:
    - name: http
      targetPort: 3000
      protocol: TCP
      port: 80
  type: ClusterIP
  selector:
    app: littlelink