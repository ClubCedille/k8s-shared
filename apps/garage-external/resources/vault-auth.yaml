apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: garage-external-auth
  namespace: garage-external
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: secret-reader
    serviceAccount: default
    audiences:
      - vault
