apiVersion: v1
data:
  compute_checksums.py: |
    #!/usr/bin/env python3
    """Computes SHA256/SHA1/MD5 for every regular file under a directory tree in
    a single read pass per file, writing a .checksums.json sidecar into each
    directory that contains files. Upstream only publishes SHA256 (ISOs) or
    SHA256+MD5 (cloud images), inconsistently -- computing all three ourselves
    keeps the mirror's checksum data uniform across every distro/artifact type.

    Usage: compute_checksums.py <root-dir>
    """
    import hashlib
    import json
    import os
    import sys

    CHUNK_SIZE = 8 * 1024 * 1024


    def hash_file(path):
        sha256, sha1, md5 = hashlib.sha256(), hashlib.sha1(), hashlib.md5()
        with open(path, "rb") as f:
            while chunk := f.read(CHUNK_SIZE):
                sha256.update(chunk)
                sha1.update(chunk)
                md5.update(chunk)
        return {"sha256": sha256.hexdigest(), "sha1": sha1.hexdigest(), "md5": md5.hexdigest()}


    def main():
        root = sys.argv[1]
        for dirpath, _dirnames, filenames in os.walk(root):
            real_files = sorted(f for f in filenames if not f.startswith("."))
            if not real_files:
                continue
            checksums = {name: hash_file(os.path.join(dirpath, name)) for name in real_files}
            with open(os.path.join(dirpath, ".checksums.json"), "w", encoding="utf-8") as out:
                json.dump(checksums, out)
            print(f"hashed {len(real_files)} file(s) in {dirpath}", file=sys.stderr)


    if __name__ == "__main__":
        main()
  generate_index.py: |
    #!/usr/bin/env python3
    """Renders a static, styled index.html for every "folder" in the mirror
    bucket, using RGW's flat key listing (there are no real directories) to
    reconstruct a tree from the "/" separators in object keys.

    Run after any sync job has pushed its files. Reads the full recursive
    listing via `rclone lsjson`, writes one index.html per directory level
    (including the bucket root) to LOCAL_OUT, then the caller is expected to
    `rclone copy` LOCAL_OUT back to the bucket.
    """
    import html
    import json
    import os
    import subprocess
    import sys
    from pathlib import PurePosixPath

    BUCKET = os.environ["BUCKET_NAME"]
    LOCAL_OUT = os.environ.get("INDEX_OUT_DIR", "/tmp/rendered-index")
    STYLE_PATH = os.environ.get("STYLE_PATH", "/opt/tooling/style.css")
    MIRROR_TITLE = "Cedille distro mirror"


    def human_size(n):
        for unit in ("B", "KiB", "MiB", "GiB", "TiB"):
            if n < 1024 or unit == "TiB":
                return f"{n:.1f} {unit}" if unit != "B" else f"{n} B"
            n /= 1024


    def list_objects():
        out = subprocess.run(
            ["rclone", "lsjson", "--recursive", f"s3:{BUCKET}", "--s3-no-check-bucket"],
            capture_output=True, check=True, text=True,
        ).stdout
        return json.loads(out)


    SKIP_NAMES = {"index.html", ".checksums.json"}


    def build_tree(objects):
        # dirs[path] = {"dirs": {name: subpath}, "files": [(name, size, modtime)], "checksums_path": str|None}
        dirs = {"": {"dirs": {}, "files": [], "checksums_path": None}}

        def ensure_dir(path):
            if path not in dirs:
                dirs[path] = {"dirs": {}, "files": [], "checksums_path": None}
                parent = str(PurePosixPath(path).parent)
                parent = "" if parent == "." else parent
                ensure_dir(parent)
                dirs[parent]["dirs"][PurePosixPath(path).name] = path
            return dirs[path]

        for obj in objects:
            if obj.get("IsDir"):
                continue
            path = PurePosixPath(obj["Path"])
            parent = str(path.parent)
            parent = "" if parent == "." else parent
            ensure_dir(parent)
            if path.name == ".checksums.json":
                dirs[parent]["checksums_path"] = obj["Path"]
            elif path.name not in SKIP_NAMES:
                dirs[parent]["files"].append((path.name, obj["Size"], obj.get("ModTime", "")))

        return dirs


    def load_checksums(path):
        if path is None:
            return {}
        out = subprocess.run(
            ["rclone", "cat", f"s3:{BUCKET}/{path}", "--s3-no-check-bucket"],
            capture_output=True, check=True, text=True,
        ).stdout
        return json.loads(out)


    CHECKSUM_ALGOS = ("sha256", "sha1", "md5")


    def checksum_cells(checksums, name):
        entry = checksums.get(name)
        return "".join(
            f'<td class="checksum">{html.escape(entry[algo])}</td>' if entry else '<td class="checksum">-</td>'
            for algo in CHECKSUM_ALGOS
        )


    def render(dir_path, entry, style_css):
        breadcrumb_parts = [p for p in dir_path.split("/") if p]
        crumbs = ['<a href="/">root</a>']
        acc = ""
        for part in breadcrumb_parts:
            acc += part + "/"
            crumbs.append(f'<a href="/{html.escape(acc)}">{html.escape(part)}</a>')
        breadcrumb = ' <span class="sep">/</span> '.join(crumbs)

        checksums = load_checksums(entry["checksums_path"])
        empty_checksum_cells = '<td class="checksum">-</td>' * len(CHECKSUM_ALGOS)

        rows = []
        if dir_path:
            parent = str(PurePosixPath(dir_path).parent)
            parent_href = "/" if parent in (".", "") else f"/{parent}/"
            rows.append(
                f'<tr><td><a class="icon-dir" href="{html.escape(parent_href)}">..</a></td>'
                f'<td class="size">-</td><td>-</td>{empty_checksum_cells}</tr>'
            )

        for name in sorted(entry["dirs"]):
            href = f"/{dir_path}/{name}/" if dir_path else f"/{name}/"
            rows.append(
                f'<tr><td><a class="icon-dir" href="{html.escape(href)}">{html.escape(name)}/</a></td>'
                f'<td class="size">-</td><td>-</td>{empty_checksum_cells}</tr>'
            )

        for name, size, modtime in sorted(entry["files"]):
            href = f"/{dir_path}/{name}" if dir_path else f"/{name}"
            modtime_short = modtime[:19].replace("T", " ")
            rows.append(
                f'<tr><td><a class="icon-file" href="{html.escape(href)}">{html.escape(name)}</a></td>'
                f'<td class="size">{human_size(size)}</td><td>{html.escape(modtime_short)}</td>'
                f'{checksum_cells(checksums, name)}</tr>'
            )

        title = f"/{dir_path}/" if dir_path else "/"
        checksum_headers = "".join(f'<th class="checksum">{algo.upper()}</th>' for algo in CHECKSUM_ALGOS)
        return f"""<!doctype html>
    <html lang="en">
    <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{html.escape(title)} — {html.escape(MIRROR_TITLE)}</title>
    <style>{style_css}</style>
    </head>
    <body>
    <div class="wrap">
    <h1>{html.escape(MIRROR_TITLE)}</h1>
    <div class="breadcrumb">{breadcrumb}</div>
    <div class="toolbar"><button type="button" onclick="document.getElementById('listing').classList.toggle('show-checksums')">Toggle checksums</button></div>
    <div class="table-scroll">
    <table id="listing">
    <thead><tr><th>Name</th><th class="size">Size</th><th>Last modified</th>{checksum_headers}</tr></thead>
    <tbody>
    {''.join(rows) if rows else f'<tr><td colspan="{3 + len(CHECKSUM_ALGOS)}" class="muted">Empty</td></tr>'}
    </tbody>
    </table>
    </div>
    <footer>Generated by the distro-mirror sync CronJobs.</footer>
    </div>
    </body>
    </html>
    """


    def main():
        style_css = open(STYLE_PATH, encoding="utf-8").read()
        objects = list_objects()
        dirs = build_tree(objects)

        for dir_path, entry in dirs.items():
            out_path = os.path.join(LOCAL_OUT, dir_path, "index.html") if dir_path else os.path.join(LOCAL_OUT, "index.html")
            os.makedirs(os.path.dirname(out_path), exist_ok=True)
            with open(out_path, "w", encoding="utf-8") as f:
                f.write(render(dir_path, entry, style_css))

        print(f"Rendered {len(dirs)} index.html file(s) under {LOCAL_OUT}", file=sys.stderr)


    if __name__ == "__main__":
        main()
  style.css: "*{box-sizing:border-box}body{background:#fff;color:#1f2328}body .breadcrumb
    a,body table a{color:#0969da}body table{border-color:#d0d7de}body table thead{background:#f6f8fa}body
    table tbody tr:hover{background:#f6f8fa}body .icon-dir{color:#9a6700}body .muted{color:#57606a}body
    footer{color:#57606a}body .toolbar button{border-color:#d0d7de;color:#1f2328}body{margin:0;padding:2rem
    1.5rem 3rem;font-family:ui-monospace,SFMono-Regular,Consolas,\"Liberation Mono\",monospace;font-size:14px;line-height:1.5}@media(prefers-color-scheme:
    dark){body{background:#0e1116;color:#c9d1d9}body .breadcrumb a,body table a{color:#58a6ff}body
    table{border-color:#30363d}body table thead{background:#161b22}body table tbody
    tr:hover{background:#161b22}body .icon-dir{color:#d29922}body .muted{color:#8b949e}body
    footer{color:#8b949e}body .toolbar button{border-color:#30363d;color:#c9d1d9}}.wrap{max-width:960px;margin:0
    auto}h1{font-size:1.1rem;font-weight:600;margin:0 0 1rem}.breadcrumb{margin-bottom:1.25rem;word-break:break-all}.breadcrumb
    a{text-decoration:none}.breadcrumb a:hover{text-decoration:underline}.breadcrumb
    .sep{margin:0 .25rem;opacity:.5}.toolbar{margin-bottom:.75rem}.toolbar button{font:inherit;font-size:.8rem;cursor:pointer;background:none;border:1px
    solid;border-radius:4px;padding:.3rem .6rem}.table-scroll{overflow-x:auto}table{width:100%;border-collapse:collapse;border:1px
    solid;border-radius:6px;overflow:hidden}table th,table td{padding:.5rem .75rem;text-align:left;border-bottom:1px
    solid;border-color:inherit;white-space:nowrap}table th{font-weight:600;font-size:.8rem;text-transform:uppercase;letter-spacing:.03em}table
    tbody tr:last-child td{border-bottom:none}table td.size,table th.size{text-align:right}table
    td.checksum,table th.checksum{display:none;font-size:.8rem}table.show-checksums
    td.checksum,table.show-checksums th.checksum{display:table-cell}table a{text-decoration:none}table
    a:hover{text-decoration:underline}.icon-dir::before{content:\"\U0001F4C1 \"}.icon-file::before{content:\"\U0001F4C4
    \"}footer{margin-top:1.5rem;font-size:.8rem}\n"
kind: ConfigMap
metadata:
  name: distro-mirror-tooling
